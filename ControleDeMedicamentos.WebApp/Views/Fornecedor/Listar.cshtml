@model List<Fornecedor>

@{
    Layout = "_Layout";
    ViewBag.Titulo = "Listagem de Fornecedores";
}

<p>
    <a class="btn btn-primary" href="Cadastrar">Cadastrar Fornecedor</a>
</p>

@if (Model.Count == 0)
{
    <div class="alert alert-warning" role="alert">
        Náo há nenhum fornecedor registrado.
    </div>
}
else
{
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-4 g-3 mt-3">

        @foreach (Fornecedor f in Model)
        {
            <div class="col">
                <div class="card h-100">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <h5>@f.Nome</h5>
                            <span class="badge text-bg-secondary">@f.Id</span>
                        </div>

                        <dl class="row mb-2">
                            <dt class="col-sm-4">Telefone</dt>
                            <dd class="col-sm-8">@f.Telefone</dd>
                            <dt class="col-sm-4">CNPJ</dt>
                            <dd class=" col-sm-8">@f.Cnpj</dd>
                        </dl>

                        <div class="d-flex gap-1 justify-content-end mt-auto">
                            <a class="btn btn-outline-primary" href="Editar/@f.Id">Editar</a>
                            <a class="btn btn-outline-danger" href="Excluir/@f.Id">Excluir</a>
                        </div>
                    </div>
                </div>
            </div>

        }
    </div>


    @* <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Nome</th>
                        <th>Telefone</th>
                        <th>CNPJ</th>
                        <th>Ações</th>
                    </tr>
                </thead>

                <tbody>
                        <tr>
                            <td>@f.Id</td>
                            <td>@f.Nome</td>
                            <td>@f.Telefone</td>
                            <td>@f.Cnpj</td>
                            <td>
                                <a class="btn btn-outline-primary" href="Editar/@f.Id">Editar</a>
                                <a class="btn btn-outline-danger" href="Excluir/@f.Id">Excluir</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table> *@
}
