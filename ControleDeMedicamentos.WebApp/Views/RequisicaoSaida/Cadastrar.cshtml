@model CadastrarRequisicaoSaidaViewModel

@{
    Layout = "_Layout";
    ViewBag.Titulo = "Cadastrar Requisição de Saída";
}

<div class="row-justufy-content-start">

    <form asp-action="Cadastrar" method="post">
        <div class="col-md-8 col-lg-6 mb-4">
            <label class="form-label" asp-for="PacienteId">Paciente</label>
            <select class="form-select" asp-for="PacienteId">
                <option value="0">Selecione um Paciente</option>
                @foreach (PacienteRequisicaoSaidaViewModel paciente in Model.Pacientes)
                {
                    <option value="@paciente.Id">@paciente.Nome</option>
                }
            </select>
        </div>

        <fieldset>
            <legend class="h5">Medicamentos requisitados</legend>

            @if (Model.MedicamentosPrescritos.Count == 0)
            {
                <div class="alert alert-warning" role="alert">Nenhum medicamento cadastrado.</div>
            }
            else
            {
                <div class="table-responsive">
                    <table class="table align-middle">
                        <thead>
                            <tr>
                                <th scope="col">Selecionar</th>
                                <th scope="col">Medicamento</th>
                                <th scope="col">Estoque</th>
                                <th scope="col">Quantidade</th>
                            </tr>
                        </thead>

                        <tbody>
                            @for (int indice = 0; indice < Model.MedicamentosPrescritos.Count; indice++)
                            {
                                MedicamentoPrescritoRequisicaoSaidaViewModel medicamentoPrescrito =
                                Model.MedicamentosPrescritos[indice];

                                <tr>
                                    <td><input asp-for="MedicamentosPrescritos[indice].MedicamentoId" type="hidden">
                                        <input class="form-check-input" asp-for="MedicamentosPrescritos[indice].Selecionado">
                                    </td>
                                    <td>@medicamentoPrescrito.NomeMedicamento</td>
                                    <td>@medicamentoPrescrito.QuantidadeEmEstoque</td>
                                    <td>
                                        <input class="form-control" asp-for="MedicamentosPrescritos[indice].Quantidade"
                                               type="number">
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </fieldset>

        <div class="d-flex gap-3 mt-4">
            <button class="btn btn-primary" type="submit">
                <i class="bi bi-check"></i>
                Cadastrar
            </button>
            <a class="btn btn-outline-secondary" asp-action="Listar">
                <i class="bi bi-arrow-return-left"></i>
                Voltar para a lista
            </a>
        </div>
    </form>

</div>
